Sources for the plugin `plugin.firebaseStorage`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.firebaseStorage" = {
            publisherId = "tech.scotth",
        },
    },
}
```
